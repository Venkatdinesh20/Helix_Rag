# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# Environment — NEVER commit secrets
.env

# Vector store: committed so CI tests have a baseline index.
# Regenerate locally with `python -m pipeline.build_index` then commit.
# vector_store/faiss_index.index
# vector_store/chunks_metadata.json

# Evaluation output
evaluation/results.json

# pytest / IDE
.pytest_cache/
.vscode/
.idea/

tools/
public/
.firebase/
